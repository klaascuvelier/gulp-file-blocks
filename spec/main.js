/* jslint node: true */
/* global describe, it, expect */
'use strict';

var _           = require('lodash'),
    FileBlocks  = require('../index.js');

describe('FileBlocks', function () {
    it('Should be defined', function () {
        expect(FileBlocks).toBeDefined();
    });
});
